// A place for shared cases

// Example:
// export const shouldViewsMessageWelcome = () => {
//   it('views chat message welcome', () => {
//     cy.get('[data-cy=chat-message-title-welcome]')
//        should('have.length', 1).and('contain', 'Welcome');
//     cy.get('[data-cy=chat-message-welcome] > :nth-child(1)').should('contain', 'Hello!');
//     cy.get('[data-cy=chat-message-welcome] > :nth-child(2)').should('contain', 'Message');
//   });
// };
